/// <reference types="vite/client" />

// Substituted by Vite's `define` from package.json at build time, so it is a
// string literal in the bundle rather than a runtime lookup. Declared here
// because TypeScript has no way to know a bundler will invent it.
declare const __APP_VERSION__: string
