export const routes = {
  // general routes
  home: "/",
  login: "/login",
  register: "/register",
  userList: "/userList/:id",

  // authenticated routes
  profile: "/profile",
  myList: "/myList",
}
