export const routes = {
  // general routes
  home: "/",
  login: "/login",
  register: "/register",

  // authenticated routes
  profile: "/profile",
  myList: "/myList",
}

export const adminRoutes = {
  games: "/admin/games",
}
