/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * TooManyHours API
 * Game tracking API. Users keep a list across three categories and share it
at a public profile URL.

There is no document-level `security` default: every protected operation
declares `BearerAuth` explicitly, so an operation without a `security`
block is genuinely public.

 * OpenAPI spec version: 0.8.0
 */
import type { UserGameCategory } from "./userGameCategory"
import type { Game } from "./game"

/**
 * One entry in a user's list. There is one row per (user, game), so category is a property of the entry rather than a separate list — which is why moving a game preserves its rating and review.
 */
export interface UserGame {
  id: number
  gameId: number
  category: UserGameCategory
  /**
   * 0.5-10 in half-steps, or null when unrated. Ten stars at half-step resolution, so 6.5 stars is a rating of 6.5.
   * @minimum 0.5
   * @maximum 10
   * @nullable
   */
  rating?: number | null
  /**
   * Plain text, at most 8000 characters.
   * @maxLength 8000
   * @nullable
   */
  review?: string | null
  /**
   * Self-reported and optional. Nothing writes this yet.
   * @nullable
   */
  hours?: number | null
  game?: Game
  createdAt: string
  updatedAt: string
}
