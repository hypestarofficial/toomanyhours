/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * TooManyHours API
 * Game tracking API. Users keep a list across three categories and share it
at a public profile URL.

There is no document-level `security` default: every protected operation
declares `BearerAuth` explicitly, so an operation without a `security`
block is genuinely public.

 * OpenAPI spec version: 0.3.0
 */
import type { UserGame } from "./userGame"

/**
 * What a stranger sees. Deliberately not APIUser — no email and no id, because nothing on the page needs either and an id would invite a second read path needing the same visibility check.
 */
export interface PublicProfile {
  username: string
  createdAt: string
  entries: UserGame[]
}
