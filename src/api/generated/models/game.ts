/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * TooManyHours API
 * Game tracking API. Users keep a list across three categories and share it
at a public profile URL.

There is no document-level `security` default: every protected operation
declares `BearerAuth` explicitly, so an operation without a `security`
block is genuinely public.

 * OpenAPI spec version: 0.6.0
 */
import type { Tag } from "./tag"

export interface Game {
  /** Surrogate key, generated. It means nothing outside this database — use igdbId to identify the game anywhere else. */
  id: number
  /** IGDB's identifier for the game. Unique. */
  igdbId: number
  title: string
  /** Absolute URL to cover art, from IGDB. */
  image: string
  /** IGDB's release type — main_game, dlc, expansion, bundle, remaster, expanded_game and so on, or unknown. Deliberately not called category, which in this API means finished/currently_playing/ want_to_play. */
  kind: string
  /**
   * IGDB's id for the game this one is an add-on of, or null. Set on remasters and expanded games as well as DLC — Skyrim Special Edition points at Skyrim — so it must never on its own be taken to mean "this is an add-on". Only kind dlc and expansion are treated that way.
   * @nullable
   */
  parentIgdbId?: number | null
  /** IGDB's short description, or "" when IGDB has none. Shown on the detail cards so somebody meeting the game cold can tell what it is. */
  summary?: string
  releaseDate: string
  /** Always present; empty when the game has none. */
  genres: Tag[]
  /** Always present; empty when the game has none. */
  themes: Tag[]
  /** Always present; empty when the game has none. */
  gameModes: Tag[]
}
