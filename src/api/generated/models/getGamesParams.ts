/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * TooManyHours API
 * Game tracking API. Users keep a list across three categories and share it
at a public profile URL.

There is no document-level `security` default: every protected operation
declares `BearerAuth` explicitly, so an operation without a `security`
block is genuinely public.

 * OpenAPI spec version: 0.2.0
 */
import type { TitleQueryParameter } from "./titleQueryParameter"
import type { GenreIdsQueryParameter } from "./genreIdsQueryParameter"

export type GetGamesParams = {
  /**
   * Case-sensitive substring match on the game title.
   */
  title?: TitleQueryParameter
  /**
   * Comma-separated genre ids, for example `1,4,12`. A string, not an array — the handler splits on commas itself. Games matching any of the ids are returned, and unparseable entries are skipped silently.
   */
  genreIds?: GenreIdsQueryParameter
  /**
   * When `true`, omits games already in the caller's list. Opt-in: this endpoint otherwise describes the whole catalog. The user is taken from the bearer token, not from the request.
   */
  excludeMine?: boolean
}
