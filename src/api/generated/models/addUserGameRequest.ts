/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * TooManyHours API
 * Game tracking API. Users keep a list across three categories and share it
at a public profile URL.

There is no document-level `security` default: every protected operation
declares `BearerAuth` explicitly, so an operation without a `security`
block is genuinely public.

 * OpenAPI spec version: 0.7.0
 */
import type { AddUserGameRequestCategory } from "./addUserGameRequestCategory"

/**
 * One IGDB id. Local catalog ids are not accepted: nothing browses the catalog now that the picker searches IGDB.
 */
export interface AddUserGameRequest {
  /** An IGDB id. If the catalog has not seen the game it is fetched from IGDB and imported before the list entry is created. An id IGDB does not know is a 404. */
  igdbId: number
  category: AddUserGameRequestCategory
  /**
   * Optional, and only valid when category is finished. There is no clear sentinel here as there is on PATCH, so the minimum is 0.5.
   * @minimum 0.5
   * @maximum 10
   */
  rating?: number
  /** Optional, and only valid when category is finished. */
  review?: string
}
